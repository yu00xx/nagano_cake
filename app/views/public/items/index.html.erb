<h2>商品一覧(全8件)</h2>
<% @items.each do |item| %>
<div>
  <%= link_to item_path(item.id) do %>
    <%= image_tag item.get_image(300,300) %>
  <% end %>
  <p><%= item.name %></p>
  <p>¥<%= item.price %></p>
</div>
<% end %>

<%= paginate @items %>